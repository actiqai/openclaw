{
  "name": "@openclaw/skill-state",
  "version": "2026.4.6",
  "private": true,
  "description": "OpenClaw plugin — per-skill profile, facts and progress kept on the user's instance",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
